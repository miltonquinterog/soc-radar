# Features

Los módulos de casos de uso se añadirán junto con consultas reales. Los directorios por dominio (`cves`, `advisories`, `kev`, `news`, `vendors` y `search`) quedan reservados deliberadamente para esa evolución; esta fase no mezcla los datos mock de presentación con acceso a datos.