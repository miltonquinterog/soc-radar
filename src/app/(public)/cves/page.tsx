import {CveCard} from "@/components/cves/cve-card";import {cves} from "@/lib/constants/mock-data";export default function Page(){return <><p className="eyebrow">Catálogo</p><h1>Vulnerabilidades CVE</h1><p className="subtitle">Vulnerabilidades normalizadas para priorización operativa.</p><div className="panel filter"><input aria-label="Filtrar CVE" placeholder="Filtrar por CVE, fabricante o término…"/></div><section className="panel"><div className="panel-body">{cves.map(x=><CveCard key={x.id} cve={x}/>)}</div></section></>}